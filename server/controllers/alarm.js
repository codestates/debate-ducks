const models = require("../models");

// 우선순위 : 낮음

module.exports = {
  // create_alarm: async (req, res) => {},
  // get_alarm: async (req, res) => {},
  // update_alarm: async (req, res) => {},
  // delete_alarm: async (req, res) => {},
};
